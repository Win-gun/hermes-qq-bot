# QQ Bot 真人感优化 PRD

## Goal

让机器人更像有稳定人格、懂群聊边界的熟人：被明确叫到时可靠回复，与自己无关时大多旁听，能自然使用既有记忆和上下文，并保持现有“小跟班”风格。

## Requirements

- 保留现有记忆、群模式、主人私聊、quiet、搜索、识图和主备账号行为。
- 统一模糊接话、讨论参与和主动发言的社交判断，减少概率驱动的突兀插话。
- 同一人拆分消息时合并理解；多人同时与 bot 对话时仍按目标逐条排队。
- bot 对自己的身份、偏好、观点和近期说法保持一致，允许带证据地自然修正观点。
- 历史检索只注入相关片段，不暴露内部档案过程。
- 新逻辑必须可影子运行、可关闭、可回退。

## Acceptance

- 明确 @、引用 bot、主人私聊和命令不会被社交判断吞掉。
- 引用消息在回复判断和最终回答中都保留被引用者、正文、消息 ID、是否 bot 消息；引用 bot 必须作为强确定接话信号。
- 多人同时回复 bot 的不同近邻消息时，每条有效回复都能独立进入群回复队列，不被“最后一条 bot 消息”覆盖。
- 同一人连续消息合并后，前序消息的引用、@、图片和消息 ID 不丢失；多人缓冲后的运行时上下文按消息时间排序。
- 桥接重启后，从本地存档恢复最近上下文与最近 bot 消息，不因运行时 Map 清空而突然失忆。
- 社交判断在决定回复前即可读取轻量上下文、引用关系和必要历史，而不是决定回复后才检索。
- 群存档摘要使用正确群 ID；滚动摘要达到长度上限后继续保留最新内容，不冻结在旧内容。
- normal 档无关消息误回复率目标低于 10%，lively 档低于 20%。
- quiet 状态发送数为 0，但归档和记忆分析继续。
- 无效 delayed 调度较基线显著下降，目标至少 70%。
- 最近回复不持续复用固定开场或模板自我介绍。
- 现有 `data/memory.json` 无破坏性迁移，旧字段继续可读。

## Non-goals

- 不伪造真人经历、肉身能力或情绪事实。
- 不通过故意错别字、随机拆句或刷屏制造真人感。
- 不新增向量数据库、前端框架或大型依赖。
- 不承诺“完美理解”所有自然语言；拿不准时应等待、检索或承认不确定，而不是编造确定关系。
